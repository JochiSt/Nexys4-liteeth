# using liteeth on NEXYS4 board
Using the liteeth ethernet core as standalone module on the Nexys4 board. The
core receives Ethernet packets and transmits the state of a counter at the same
time. Python scripts for testing ethernet RX and TX are available as well.

# Disclaimer
The simulation at `sim` using cocotb and scapy does not work (yet).